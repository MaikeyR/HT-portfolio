model Project {
  id            String   @id @default(uuid())
  title         String
  description   String
  members       Person[]
  startDate     DateTime
  endDate       DateTime
  technologies  Technology[]
}